@model IEnumerable<BooksNet.Models.Publisher>

@{
  ViewBag.Title = "Index";
  Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="col-md-12">
    <div class="card">

        <div class="card-header card-header-icon" data-background-color="red">
            <i class="material-icons">print</i>
        </div>
        <h4 class="card-title">Publishers</h4>
        <br />
        <div class="card-content table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.Name)</th>
                        <th>@Html.DisplayNameFor(model => model.Address)</th>
                        <th>@Html.DisplayNameFor(model => model.Books)</th>
                        <th class="text-right">Actions</th>
                    </tr>
                </thead>
                <tbody>

                    @foreach (var item in Model)
                    {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Address)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Books.Count)
                        </td>

                        <td class="td-actions text-right">
                            <a href="@Url.Action("Edit", new { id = item.Id })" class="btn btn-success btn-round"> <i class="material-icons">edit</i></a>
                            <a href="@Url.Action("Delete", new { id = item.Id })" class="btn btn-danger btn-round"> <i class="material-icons">close</i></a>
                        </td>
                    </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<a href="@Url.Action("Create")" class=" btn btn-danger add fixed-corner"><i class="material-icons">add</i></a>